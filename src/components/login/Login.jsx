import styled from "styled-components";

const LoginId = styled.placeholder`
    position: absolute;
    width: 20px;
    height: 20px;
`

function Login() {
  return (
    <LoginId/>
  );
};

export default Login;